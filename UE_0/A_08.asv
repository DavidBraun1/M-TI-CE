%Macht z zu einer Variabel für Funktionen
syms z
x = 1;
h = logspace(-1,-15,15);

%Finite Diferenz
D = (sin(x+h)-sin(x))./(h);
fprintf("Näherung für h=%.e: %.6f\n",[h; D])

%Ableitung von sin(z)
double(subs(diff(sin(z)), z, x))